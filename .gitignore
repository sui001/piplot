__pycache__/
*.pyc
lyre_output.svg
plan.json
*.egg-info/

# This Pi's own machines, with its own USB paths and envelopes. Not the repo's
# business, and a deploy must never overwrite it. See machines.example.json.
machines.json
